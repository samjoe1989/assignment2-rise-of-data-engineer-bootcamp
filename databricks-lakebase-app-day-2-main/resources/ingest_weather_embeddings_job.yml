# Databricks Job that runs ingest_weather_embeddings.py every 2 hours
# to fetch weather data and generate embeddings for semantic search.
#
# Deploy: databricks bundle deploy -t dev
# Run once manually: databricks bundle run ingest_weather_embeddings_job -t dev

resources:
  jobs:
    ingest_weather_embeddings_job:
      name: "[${bundle.target}] Ingest Weather Embeddings"

      # Runs every 2 hours
      schedule:
        quartz_cron_expression: "0 0 */2 * * ?"
        timezone_id: "UTC"
        pause_status: PAUSED  # flip to UNPAUSED once you've validated a manual run

      email_notifications:
        on_failure:
          - ${workspace.current_user.userName}

      max_concurrent_runs: 1

      job_clusters:
        - job_cluster_key: default_cluster
          new_cluster:
            spark_version: "15.4.x-scala2.12"
            node_type_id: "i3.xlarge"
            num_workers: 2
            data_security_mode: SINGLE_USER

      tasks:
        - task_key: ingest_weather_embeddings
          job_cluster_key: default_cluster
          notebook_task:
            notebook_path: ../notebooks/ingest_weather_embeddings.py.py
            base_parameters:
              weather_documents_table_name: "weather_documents"
              embeddings_table_name: "weather_documents_embeddings"
              embedding_model: "sentence-transformers/all-MiniLM-L6-v2"
              news_fetch_limit: "50"
              max_requests_per_minute: "5"
              chunk_size: "800"
              chunk_overlap: "100"
